//notación: los mas importantes son
//; -> Nos sirve para "delimitar"el final de una línea. ej:
const b = 4;
b + 4;

// . -> Se utiliza en los objetos para acceder a los atributos. ej:
   //movil.anchura

// [corchetes] -> Se utilizan para las listas, arreglos o arrays. ej:
const ar =[1, 2, 3, 4]
console.log(ar[2])

//(parentesis) -> Se utilizan para funciones. ej:
function suma(a, b){
    //se escribe la función.
}

// {llaves} -> Llave para objetos, funciones y estructuras de control. ej:
const movil ={ 
    anchura: 5,
    altura: 10,
}

if (true) {
    //todo lo que haya entre llaves

    const constante2= "hola"

}
